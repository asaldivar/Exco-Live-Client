import React, { Component } from 'react';

// Note: this component could be used for both Check In and Check Out.
class AttendeeList extends Component {
  render() {
    return (
      <div>
        {/* Prompt: Display a list of all employees (users) from the same store as current user.
            Allow the user to select any employee from list to check them in/out; to select an employee, user has to enter the last 4 digits of their username.*/}
        <div className="prompt">Make attendee list dynamic below.</div>

        <div>
          
        </div>
        
      </div>
    )
  };
};

export default AttendeeList;